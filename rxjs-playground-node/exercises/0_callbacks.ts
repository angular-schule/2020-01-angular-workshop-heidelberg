function connect1(callback) {
  setTimeout(() => callback(), 2000);
}

function read1(callback) {
  setTimeout(() => callback(), 2000);
}

function close1(callback) {
  setTimeout(() => callback(), 2000);
}

/**********************************/

